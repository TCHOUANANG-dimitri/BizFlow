# packages/shared

Contrat API généré par le backend. **Lecture seule pour l'agent OpenCode.**

- `openapi.json` : schéma OpenAPI exporté depuis FastAPI. Régénéré par Claude Code au fil du
  développement du backend — ne pas éditer à la main. S'il te manque un endpoint/champ pour
  avancer côté `apps/`, note-le dans `opencode.md` (section "Points ouverts") plutôt que d'éditer
  ce fichier.
